apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: homework
  name: webserver-pvc
spec:
  #  storageClassName: "standard"
  storageClassName: "low-latency"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi

