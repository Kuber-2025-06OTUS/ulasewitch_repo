apiVersion: v1
kind: Service
metadata:
  name: webserver-service
  namespace: homework
spec:
  selector:
    app: webserversel
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 8000
  type: ClusterIP

